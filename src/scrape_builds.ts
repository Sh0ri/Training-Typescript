"use strict"

// imports
import * as typeGuards from "./scrapper"

const getAllBuilds = (): (typeGuards.IBuild[] | void) => {
    // selectors
    const FULL_BUILD_SELECTOR = "#content > div > div > div.champion-profile-page > div > div._grid-3._grid-columns"
}
